import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard - CN Timesheet',
  description: 'CN Timesheet',
};

const Dashboard = () => {
  return <div> Dashboard </div>;
};

export default Dashboard;
